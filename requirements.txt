accelerate==1.6.0
datasets
safetensors
transformers>=4.51.3
trl>=0.13.0
hf_transfer
torch>=2.0.0
deepspeed>=0.15.0
kernels  # Required for gpt-oss models
peft  # For LoRA/QLoRA if needed
wandb  # For experiment tracking
